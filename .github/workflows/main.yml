name: Python CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -e .

    - name: Lint with Flake8
      run: |
        pip install flake8
        # Stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Run Unit Tests
      run: |
        python -m unittest discover tests

    - name: Run Automated Evaluation Suite (Against Golden Dataset)
      env:
        # Secure injection of Gemini API Key for evaluation
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        # Suppress interactive confirmation prompts for non-blocking execution in CI
        CI: "true"
      run: |
        python -m tutor_agent.eval_suite
